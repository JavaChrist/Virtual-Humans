# Character Validation Workflow

Repeatable process for validating a character SDK against the standards and schemas. See `workflow.json` for the declarative definition and `checklist.md` for the validation gates. Draft: steps to be completed.
