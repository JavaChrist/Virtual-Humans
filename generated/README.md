# Generated

This directory contains generated reports, indexes and manifests.

Files stored here must not be treated as authoritative source files.

Authoritative content remains in `core/`, `schema/`, `templates/` and `characters/`.
