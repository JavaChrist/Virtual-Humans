# Examples

## Purpose

Illustrative, syntactically valid example payloads for the main data structures of the SDK.

## Can contain

- Example JSON documents aligned with the schemas in `schema/`.

## Must not contain

- Authoritative character data.
- Credentials or secrets.

## Relationships

- Mirror the structures defined in `schema/`.
- Can be used as references by `docs/` and `validators/`.
